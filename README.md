check_kernel_parameters.sh
==========================

Prints out the kernel's command line as embedded in boot.img

check_kernel_version.sh
=======================

Prints the kernel's version from the kernel file
